<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0B0B18"
    android:orientation="vertical"
    tools:context=".admin.AdminProfilesFragment">

    <!-- Header Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@android:color/white"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp">

        <!-- Back Button -->
        <ImageButton
            android:id="@+id/button_back"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="@android:color/transparent"
            android:contentDescription="Back"
            android:src="@drawable/ic_arrow_back"
            app:tint="#000000" />

        <!-- Wingman Logo -->
        <ImageView
            android:id="@+id/image_logo"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="8dp"
            android:contentDescription="Wingman Logo"
            android:src="@drawable/wingman_logo" />

        <!-- Search Bar -->
        <TextView
            android:id="@+id/search_bar_profiles"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:background="@drawable/search_box_bg"
            android:drawableStart="@drawable/ic_search"
            android:drawablePadding="6dp"
            android:gravity="center_vertical"
            android:text="Scroll To Search Profiles"
            android:inputType="text"
            android:paddingHorizontal="12dp"
            android:textColor="#000000"
            android:textColorHint="#888888" />
    </LinearLayout>

    <!-- Profile List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_admin_profiles"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp"
        tools:listitem="@layout/list_item_admin_profile" />
</LinearLayout>
